self.webpackChunk([6],{GKJL:function(e,n,t){"use strict";t.r(n),t.d(n,"daySixPartTwo",function(){return r});const r="const fileReader = new MyFileReader(input);\nconst answeredYesQuestions = fileReader.readEntriesSeperatedByWhiteline(\n  (lines) => {\n    const asSet = lines.map((line) => new Set(line.split('')));\n    if (asSet.length === 1) return asSet[0].size;\n    const letterCountMap = new Map<string, number>();\n    asSet.forEach((set) => {\n      set.forEach((letter) => {\n        const existingNumber = letterCountMap.get(letter);\n        if (!existingNumber) {\n          letterCountMap.set(letter, 1);\n        } else {\n          letterCountMap.set(letter, existingNumber + 1);\n        }\n      });\n    });\n    return Array.from(letterCountMap.entries()).reduce((prev, [, count]) => {\n      if (count === asSet.length) return prev + 1;\n      return prev;\n    }, 0);\n  }\n);\n\nreturn answeredYesQuestions.reduce((prev, next) => prev + next, 0);"}});