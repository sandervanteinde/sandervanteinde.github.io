self.webpackChunk([25],{FVPn:function(e,n,t){"use strict";t.r(n),t.d(n,"dayThreePartTwo",function(){return r});const r="const fileReader = new MyFileReader(input);\nconst lines = fileReader.readLineByLine();\nconst maxLine = lines.reduce((prev, line) => Math.max(prev, line.length), 0);\nconst TREE = '#';\nfunction traverse(leftStep: number, downStep: number): number {\n  let currentPos = 0;\n  let treesEncountered = 0;\n  for (let i = 0; i < lines.length; i += downStep) {\n    if (lines[i][currentPos] === TREE) treesEncountered++;\n    currentPos = (currentPos + leftStep) % maxLine;\n  }\n  console.log(leftStep, '/', downStep, ':', treesEncountered);\n  return treesEncountered;\n}\nconst multiplied = [\n  [1, 1],\n  [3, 1],\n  [5, 1],\n  [7, 1],\n  [1, 2],\n].reduce((prev, [left, down]) => traverse(left, down) * prev, 1);\n\nreturn multiplied;"}});